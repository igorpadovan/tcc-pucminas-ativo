version: '2'
services:
  ativo-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/Ativo/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=Ativo
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
